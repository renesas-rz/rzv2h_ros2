diff --git a/src/codec.cpp b/src/codec.cpp
index 9536d81..9b86980 100644
--- a/src/codec.cpp
+++ b/src/codec.cpp
@@ -99,7 +99,7 @@ sensor_msgs::msg::Image::SharedPtr decodeCompressedDepthImage(
       }
       catch (cv::Exception& e)
       {
-        RCLCPP_ERROR(logger, e.what());
+        RCLCPP_ERROR(logger, "%s", e.what());
         return sensor_msgs::msg::Image::SharedPtr();
       }
 
@@ -142,7 +142,7 @@ sensor_msgs::msg::Image::SharedPtr decodeCompressedDepthImage(
       }
       catch (cv::Exception& e)
       {
-        RCLCPP_ERROR(logger, e.what());
+        RCLCPP_ERROR(logger, "%s", e.what());
         return sensor_msgs::msg::Image::SharedPtr();
       }
 
@@ -207,7 +207,7 @@ sensor_msgs::msg::CompressedImage::SharedPtr encodeCompressedDepthImage(
     }
     catch (cv_bridge::Exception& e)
     {
-      RCLCPP_ERROR(logger, e.what());
+      RCLCPP_ERROR(logger, "%s", e.what());
       return sensor_msgs::msg::CompressedImage::SharedPtr();
     }
 
@@ -266,7 +266,7 @@ sensor_msgs::msg::CompressedImage::SharedPtr encodeCompressedDepthImage(
       }
       catch (cv::Exception& e)
       {
-        RCLCPP_ERROR(logger, e.msg.c_str());
+        RCLCPP_ERROR(logger, "%s", e.msg.c_str());
         return sensor_msgs::msg::CompressedImage::SharedPtr();
       }
     }
@@ -282,7 +282,7 @@ sensor_msgs::msg::CompressedImage::SharedPtr encodeCompressedDepthImage(
     }
     catch (Exception& e)
     {
-      RCLCPP_ERROR(logger, e.msg.c_str());
+      RCLCPP_ERROR(logger, "%s", e.msg.c_str());
       return sensor_msgs::msg::CompressedImage::SharedPtr();
     }
 
